let currentLeft = 0;
let newLeft = 0;

export default class Animator {
	/**
	 * Start animating an element to a new horizontal offset with.
	 *
	 * @param {Element} element		The element that should be animated.
	 * @param {Number} newLeft		The new left offset that the element should animate to.
	 * @param {Number} duration		The amount of time in seconds that the animation should take.
	 */
	static animateElementToHorizontalOffset(element, newLeft, duration) {

	}
}
